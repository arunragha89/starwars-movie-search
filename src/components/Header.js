import React from "react";
import SearchBar from "./SearchBar";

const Header = () => {
  return (
    <header className="flex justify-items-center m-4">
      <SearchBar />
    </header>
  );
};

export default Header;
